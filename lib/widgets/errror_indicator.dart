import 'package:flutter/material.dart';

class ErrrorIndicator extends StatelessWidget {
  const ErrrorIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Something won't wrong!"));
  }
}
